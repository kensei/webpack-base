{
  "name": "",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run rimraf && cross-env NODE_ENV=production webpack",
    "watch": "webpack --watch",
    "start": "webpack-dev-server",
    "rimraf": "rimraf dist/*"
  },
  "dependencies": {},
  "devDependencies": {
  }
}
